// export const isValidCUETEmail = (email) => {
//   // Simple regex for @cuet.ac.bd domain. Adjust if specific format like student ID is needed.
//   return /^[a-zA-Z0-9._%+-]+@cuet\.ac\.bd$/.test(email);
// };

// Add other validation functions here if needed
// export const isValidPassword = (password) => { ... };
